#include <iostream>
#include <string>

//Se escriben todos los metodos a utilizar en el fdisk

using namespace std;

class Particiones{
public:
    void analizador_fd(string cad);
    void write_partition();
    void ejecudar_particion();
};